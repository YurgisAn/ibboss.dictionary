import React, { HTMLAttributes } from 'react';
declare type Status = 'info' | 'error' | 'success' | 'warning';
export interface NotificationProps extends Omit<HTMLAttributes<HTMLDivElement>, 'title' | 'id'> {
    /** Статус notification */
    status?: Status;
    /** Заголовок notification */
    title?: React.ReactNode;
    /** Название для ссылки */
    linkText?: string;
    /** Url ссылки */
    href?: string;
    /** Переключатель видимости иконки "Close" */
    isClosable?: boolean;
    /** Закрытие notification */
    onClose?: () => void;
    /** Переключатель видимоти статусных иконок */
    displayStatusIcon?: boolean;
}
export declare const Notification: {
    ({ status, title, linkText, href, displayStatusIcon, isClosable, onClose, children, ...props }: React.PropsWithChildren<NotificationProps>): JSX.Element;
    displayName: string;
};
export {};
//# sourceMappingURL=index.d.ts.map