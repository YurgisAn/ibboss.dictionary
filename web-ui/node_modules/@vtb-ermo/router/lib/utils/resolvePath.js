"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePath = void 0;
var resolveRouteToString_1 = require("./resolveRouteToString");
var resolvePath = function (routesConfig, to) {
    switch (typeof to) {
        case 'object':
            if (Array.isArray(to)) {
                return to.map(function (item) {
                    return typeof item === 'string' ? item : resolveRouteToString_1.resolveRouteToString(routesConfig, item);
                });
            }
            else {
                return typeof to.pathname === 'object'
                    ? __assign(__assign({}, to), { pathname: resolveRouteToString_1.resolveRouteToString(routesConfig, to.pathname) }) : resolveRouteToString_1.resolveRouteToString(routesConfig, to);
            }
        case 'string':
        case 'function':
            return to;
        default:
            return undefined;
    }
};
exports.resolvePath = resolvePath;
//# sourceMappingURL=resolvePath.js.map