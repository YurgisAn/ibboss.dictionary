import { routesConfig } from '../routes';
declare type OmitDistributive<T, K extends PropertyKey> = T extends any ? (T extends object ? OmitRecursively<T, K> : T) : never;
declare type OmitRecursively<T extends any, K extends PropertyKey> = Omit<{
    [P in keyof T]: OmitDistributive<T[P], K>;
}, K>;
export declare type RoutesConfig = typeof routesConfig;
export declare type RoutesKeysConfig = OmitRecursively<RoutesConfig, 'params'>;
export {};
