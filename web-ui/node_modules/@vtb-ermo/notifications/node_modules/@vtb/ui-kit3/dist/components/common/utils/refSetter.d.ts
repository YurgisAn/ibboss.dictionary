import React from 'react';
/**
 * Useful when passing refs from outside, but want to handle internal ref.
 * Keeps passed refs in sync, by creating ref callback
 * @param refs
 */
export declare function refSetter<T>(...refs: React.ForwardedRef<T>[]): (instance: T | null) => void;
//# sourceMappingURL=refSetter.d.ts.map