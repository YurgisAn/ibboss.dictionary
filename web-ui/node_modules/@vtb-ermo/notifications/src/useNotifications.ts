import { useContext } from 'react';
import { NotificationsContext } from './NotificationsContext';

export const useNotifications = () => {
    const { publish, unPublish } = useContext(NotificationsContext);
    return { publish, unPublish };
};