import styled from 'styled-components';
import { TransitionList } from 'transition-list';
import { ContentWrapper } from './components/NotificationItem/styled';

export const NotificationAlertsContainer = styled(TransitionList)`
    position: fixed;
    bottom: 12px;
    right: 16px;
    z-index: 999;
    > div:not(:last-child) {
        margin-bottom: 12px;
    }
    ${ContentWrapper} {
        div {
            display: none;
        }
    }
`;