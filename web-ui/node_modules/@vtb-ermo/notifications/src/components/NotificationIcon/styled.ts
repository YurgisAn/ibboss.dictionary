import styled from 'styled-components';
import { Badge } from '@vtb/ui-kit3';
import { ReactComponent as Notification } from '@openvtb/admiral-icons/build/system/NotificationsSolid.svg';

export const NotificationIconStyled = styled(Notification)`
    height: 28px;
    path {
        fill: ${({ theme }) => theme.color.basic.tertiary};
    }
`;

export const BadgeStyled = styled(Badge)``;

export const NotificationIconWrapper = styled.div`
    margin: 12px 8px 0 12px;
    cursor: pointer;
    ${BadgeStyled} {
        position: relative;
        top: -20px;
        left: -8px;
    }
`;