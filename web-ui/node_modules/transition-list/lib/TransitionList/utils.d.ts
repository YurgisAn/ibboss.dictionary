import type { Key, ReactNode } from 'react';
import type { ChildHeight, RenderChild } from './types';
export declare const calculateTopPosition: (children: ReactNode, childrenHeight: Array<ChildHeight>, key: Key | null) => number;
export declare const mergeChildrenForRender: (prevChildren: ReactNode, nextChildren: ReactNode, childrenHeight: Array<ChildHeight>) => Array<RenderChild> | [];
