import { Key, ReactNode } from 'react';

export type ChildHeight = {
    key: number | string
    height: number
}

export type RenderChild = {
    key: Key | null
    child: ReactNode
    height?: number
    newChild?: boolean
    removedChild?: boolean
    translateY?: number
}