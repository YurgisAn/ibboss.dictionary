import { FC, MouseEvent, KeyboardEvent } from 'react';
import type { LabelPosition, Size } from './SwitchSlider';
export interface ISwitchProps {
    /** Состояние свитча: выставлен/не выставлен */
    checked?: boolean;
    /** Коллбэк на изменение состояния. Срабатывает при клике или переходу по табу */
    onChange: (event: MouseEvent<HTMLDivElement> | KeyboardEvent<HTMLDivElement>, checked: boolean) => void;
    /** Подпись к свитчу */
    label?: string;
    /** Расположение подписи */
    labelPosition?: LabelPosition;
    /** Отключение свитча */
    disabled?: boolean;
    /** Размер свитча */
    size?: Size;
    /** Имя класса для переопределения стилей */
    className?: string;
    /** data-test-id атрибут для тестирования компонента */
    dataTestId?: string;
}
export declare const Switch: FC<ISwitchProps>;
//# sourceMappingURL=index.d.ts.map