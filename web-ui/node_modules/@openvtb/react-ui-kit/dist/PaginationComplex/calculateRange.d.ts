export declare const calculateRange: ({ itemsPerPage, lastPage, page, totalItems, }: {
    itemsPerPage: number;
    lastPage: number;
    page: number;
    totalItems: number;
}) => {
    start: number;
    end: number;
};
//# sourceMappingURL=calculateRange.d.ts.map